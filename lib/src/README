Sources from libsvm-2.88 plus:

- info() is silenced
- __declspec(dllexport) magic in svm.h for windows
- added model_w2 function that returns the norm of the normal vector
  of the separating hyperplane
